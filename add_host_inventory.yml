---
#- hosts: bastion 
#  gather_facts: false
#  tasks:
#    - ansible.builtin.shell: hostname
#      register: host_name
#
#    - ansible.builtin.debug:
#        var: host_name
#
#    - ansible.builtin.debug:
#        var: host_name.stdout
#
#    - ansible.builtin.add_host:
#        name: "{{ item }}"
#        groups: new-machine
#      with_items:
#        - "{{ new_host }}"

- hosts: all
  connection: local
  gather_facts: false
  tasks:
    - ansible.builtin.debug: 
        msg: "{{ lookup('env', 'FOREMAN_INI_PATH') }}"

    - shell: env

    - shell: set

    - shell: df -h

    - shell: ls -lha

    - shell: find .
