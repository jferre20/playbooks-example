---
- hosts: all
  connection: local
  tasks:
    - debug:
        var: hostvars['inventory_hostname']

    - debug:
        msg: |
          "{{ item.connection }}"
          "{{ item.token_ocp }}"
      loop: "{{ inventory_hostname }}"
