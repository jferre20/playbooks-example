---
- hosts: all
  connection: local
  tasks:
    - debug:
        var: inventory_hostname 

    - debug:
        msg: |
          "{{ hostvars['item'].connection }}"
          "{{ hostvars['item'].token_ocp }}"
      loop: "{{ inventory_hostname }}"
