---
- hosts: all
  connection: local
  tasks:
    - debug:
        msg: |
          "{{ hostvars['localhost'].connection }}"
          "{{ hostvars['localhost'].token_ocp }}"
          "{{ inventory_hostname.connection }}"
