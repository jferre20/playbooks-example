---
- hosts: all
  connection: local
  tasks:

    - debug:
        msg: |
          "{{ hostvars[item].environment['connection'] }}"
          "{{ hostvars[item].environment['token_ocp'] }}"
      loop: 
        - "{{ inventory_hostname }}"
