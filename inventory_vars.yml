---
- hosts: all
  connection: local
  tasks:
    - debug:
        var: ansible_facts

    - debug:
        msg: |
          "{{ hostvars['item'].connection }}"
          "{{ hostvars['item'].token_ocp }}"
      loop: "{{ group['all'] }}"
